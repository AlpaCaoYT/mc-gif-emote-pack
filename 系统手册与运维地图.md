# 动图资源包生成器 · 系统手册与运维地图

> 用途：快速了解项目结构、排查常见问题、接手维护。面向非程序员，照表查即可。

## 1. 项目定位

把 `素材/` 里的 **GIF / 普通 PNG / WebP 表情包** 随机替换成 Minecraft Java 版原版方块/物品贴图，自动拼成会动的竖长图 + 动画配置，打包成可直接安装的 ZIP 资源包。

- 只改**方块/物品**贴图；实体、GUI 不受支持（游戏机制限制）
- 输出在 `输出/` 文件夹，丢进 `.minecraft/resourcepacks` 启用

## 2. 文件地图

| 路径 | 作用 | 改不动 |
|---|---|---|
| `资源包工具/动图资源包生成器.py` | 主程序（图形界面 + 核心逻辑） | 基本都在这 |
| `资源包工具/启动生成器.bat` | 双击启动（调 python 跑上面脚本） | 一般不动 |
| `资源包工具/vanilla_textures_1.21.10.json` | 原版方块/物品贴图清单（自带，无需解包游戏） | 加版本时动 |
| `资源包工具/生成器配置.json` | 界面上次填的值（自动生成） | 可删，重开恢复默认 |
| `素材/` | 你的表情包（GIF/PNG/WebP） | 你维护 |
| `输出/` | 生成的 ZIP + 报告 + 预览图 + mapping.json | 生成的 |

> `输出/` 和 `素材/` 已在 `.gitignore` 排除，不会误提交大文件。

## 3. 核心流程（出问题时顺着查）

```
素材(GIF/PNG/WebP)
  → extract_frames 逐帧提取 + 按真实时长算动画刻度
  → build_strip 竖向拼成正方形长图（高=分辨率×帧数）
  → 按分配策略把每个方块/物品对应一个表情
  → 写 PNG + .png.mcmeta
  → 打包 ZIP（STORED，PNG 已压缩不再二次压）
```

关键数字：**长图高度 = 分辨率 × 帧数**。超过游戏贴图上限（约 16384px）会黑屏，工具已按分辨率自动限制单张最大帧数。

## 4. 配置项速查（界面每一项对应什么）

| 界面项 | 配置键 | 说明 |
|---|---|---|
| GIF 素材文件夹 | `gif_dir` | 默认 `素材/` |
| 输出文件夹 | `out_dir` | 默认 `输出/` |
| 资源包名称 | `pack_name` | 决定 ZIP 和附属文件名 |
| 资源包简介 | `description` | 支持 `§6§l` 等 MC 颜色代码 |
| 游戏版本 | `mc_version` | 控制 `pack_format` 兼容性数字 |
| 贴图分辨率 | `resolution` | 64 / 128 / 256 |
| 替换范围 | `scope` | 常见方块 / 全部方块 / 方块+物品 |
| 分配策略 | `strategy` | 全随机 / 优先常见方块用不同表情 |
| 透明底处理 | `alpha_mode` | 填白色底 / 保留透明 |
| 随机种子 | `seed` | 相同种子 → 相同结果，方便同步 |
| 单张GIF最大帧数 | `max_frames` | 超过则均匀抽帧，保持总时长 |
| 自定义排除规则 | `exclude_rules` | 逗号/换行分隔，`*` 通配 |
| 增量更新 | `incremental` | 勾选后沿用上次分配 |

## 5. 功能说明

- **自定义排除规则**：界面填 `diamond_block,glass` 或 `*` 通配（如 `red_*`），对所有模式生效；内置排除（水/岩浆/树叶/草顶等）仍自动应用。
- **增量更新**：勾选后读取 `输出/<包名>_mapping.json`，沿用上次分配，只把**新增表情**注入到原本"重复占用"的方块上，几秒出包。**新增素材后想不打乱旧世界观感时用**；普通重生成不勾。
- **WebP / 普通 PNG**：素材搜索含 `.gif .png .webp`；单帧 PNG = 静态贴图，WebP 动图按帧动画。
- **效果预览图**：生成后出 `<包名>_预览图.png`，采样若干方块拼缩略图（非游戏内实拍，仅看分布）。
- **失败清单**：任一贴图生成失败时写入 `<包名>_失败清单.txt`，并照常完成其余。
- **预计剩余时间**：进度日志每 10 个显示 `预计剩余 N 秒`。
- **分配策略**：`优先常见方块用不同表情` 在"方块+物品"模式下让常见方块先吃满不同表情。

## 6. 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 草顶/树叶变绿 | 被游戏染色，本应自动跳过 | 确认用的是新版工具；旧包重生成 |
| 某贴图黑屏/不加载 | 长图超高超出游戏上限 | 降低分辨率或帧数；工具已自动限帧 |
| 生成巨慢 | 旧版对每个格子重复编码 + ZIP 二次压缩 | 已优化（相同表情只编码一次，ZIP 用 STORED），现在约 30 秒 |
| 想去掉某些方块 | 没用排除规则 | 界面填自定义排除规则 |
| 增量更新报错/无效 | 没找到 mapping.json 或范围/分辨率变了 | 确认 `输出/<包名>_mapping.json` 存在且 scope/分辨率一致 |
| 资源包在游戏里提示不兼容 | 版本号选错 | 选你游戏对应版本（1.21.9-1.21.10 选默认） |
| 文件被占用无法删除 | Windows 句柄未释放 | 关闭预览图/资源管理器占用，工具已修复增量模式的句柄泄漏 |

## 7. 扩展指南

- **加游戏版本模板**：在 `动图资源包生成器.py` 的 `VERSION_TEXTURE_MAP` 增加 `"版本号": "清单文件名.json"`，并把对应清单放进 `资源包工具/`。方块名从 1.13 起基本稳定，1.20~1.21.10 一套清单通用。
- **加素材格式**：在 `generate_pack` 的 `gifs = sorted(...)` 那行加 `rglob("*.新后缀")`，并确保 Pillow 能读（如 avif 需额外装 `pillow-avif-plugin`）。
- **改常见方块清单**：编辑 `COMMON_BLOCKS` 列表（变量在脚本顶部）。
- **改内置排除**：编辑 `EXCLUDE_BLOCK` / `EXCLUDE_ITEM`。

## 8. 接手须知

- 主逻辑全在 `动图资源包生成器.py`，无外部依赖（仅需 `pillow`：`pip install pillow`）。
- 打包用 `ZIP_STORED` 是有意为之（PNG 已压缩，再压纯浪费且极慢），不要改回 `DEFLATED`。
- `mapping.json` 是增量更新的命根子，误删只是下次变全量，不影响功能。
- 提交前确认 `git status` 不含 `素材/`、`输出/` 大文件。
